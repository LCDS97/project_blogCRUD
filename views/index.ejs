<%- include ('partials/header.ejs') %>
<%- include ('partials/navbar-home.ejs') %>
<body>
    <div class="container">
        <hr>
        <div class="jumbotron">
            <h1 class="display-4">Bem-vindo</h1>
            <p class="lead">Este é um simples blog pessoal com base no modelo CRUD</p>
            <hr class="my-4">
            <p>Estou utilizanod Node JS para criação de um blog baseado no modelo CRUD e algumas bibliotecas como bootstrap e express</p>
            <a class="btn btn-primary btn-lg" href="#" role="button">Chorasti</a>
          </div>
    <% articles.forEach(article => { %>
            <div class="card">
                <div class="card-header">
                    <h2><%= article.title %></h2>
                </div>
                <div class="card-body">
                    <a href="/<%= article.slug %>" class="btn btn-primary"> Ler Artigo</a>
                </div>
            </div>
            <br>
        <% }) %>

    </div>

</body>

<%- include ('partials/footer.ejs') %>